# Themes

- `default/layout.css`: current default app shell layout theme

MarkHola loads `themes/default/layout.css` at runtime when available.
In development, edit the repository `themes/default/layout.css` directly.
In the packaged macOS app, the same file is copied into `MarkHola.app/Contents/Resources/themes/default/layout.css`.

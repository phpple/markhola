# Open Multiple Example 1

Use `Command + O` and multi-select this file together with `examples/open-multiple-2.md`.

This file exists to verify that the Open File dialog can open multiple Markdown documents in one action.

